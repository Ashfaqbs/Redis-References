version: '3.8'

services:
  redis:
    image: redis:latest
    container_name: my-redis-db
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: ["redis-server"] # Starts Redis server

volumes:
  redis_data:
    driver: local
